import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mission-classroom-ionic',
  webDir: 'dist'
};

export default config;
